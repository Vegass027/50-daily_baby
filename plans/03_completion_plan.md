# План завершения этапа 3: PumpFun + Лимитные ордера

**Дата:** 2025-01-09  
**Статус:** 🔄 В разработке

---

## 🎯 Цели

✅ Реализовать полноценную поддержку PumpFun с MEV защитой  
✅ Создать систему виртуальных лимитных ордеров для PumpFun  
✅ Интегрировать все компоненты в Telegram бота

---

## 📋 ЗАДАЧИ

### ЧАСТЬ 1: PriorityFeeManager (Оптимизация скорости)

**Файл:** `src/trading/managers/PriorityFeeManager.ts`

**Требования:**
- Реализовать `getRecentPrioritizationFees()` для динамического расчета комиссий
- Поддержка трех стратегий скорости: low, normal, aggressive
- Кэширование результатов (5 секунд)
- Обработка ошибок с fallback на фиксированное значение

**Алгоритм:**
```
Low: medianFee × 1.0
Normal: medianFee × 1.15 (рекомендуется)
Aggressive: top 10% fee
```

---

### ЧАСТЬ 2: PumpFunStrategy с MEV защитой

**Файл:** `src/trading/strategies/solana/PumpFunStrategy.ts`

**Требования:**
- Использовать встроенную Jito поддержку из PumpFun SDK
- Добавить параметр `useJito` в executeSwap
- Реализовать динамический Jito tip на основе размера сделки
- Добавить логирование MEV защиты

**API PumpFun SDK:**
```typescript
// Jito buy
await sdk.jito!.buyJito(
  wallet,
  mint.publicKey,
  BigInt(amount),
  slippageBps,
  tipLamports,
  PRIORITY_FEE,
  "confirmed"
);

// Jito sell
await sdk.jito!.sellJito(
  wallet,
  mint.publicKey,
  BigInt(amount),
  slippageBps,
  tipLamports,
  PRIORITY_FEE,
  "confirmed"
);
```

---

### ЧАСТЬ 3: Интерфейс ILimitOrderManager

**Файл:** `src/trading/managers/ILimitOrderManager.ts`

**Требования:**
- Универсальный интерфейс для всех типов лимитных ордеров
- Поддержка разных DEX (PumpFun, Jupiter)
- Единый API для создания, отмены, мониторинга ордеров

**Интерфейс:**
```typescript
export enum OrderType {
  BUY = 'buy',
  SELL = 'sell',
}

export enum OrderStatus {
  PENDING = 'pending',
  FILLED = 'filled',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
}

export interface LimitOrderParams {
  tokenMint: string;
  orderType: OrderType;
  amount: number; // в базовых единицах
  price: number; // цена в SOL за 1 токен
  slippage?: number;
  takeProfitPercent?: number; // автоматический take profit
  stopLossPercent?: number; // автоматический stop loss
}

export interface LimitOrder {
  id: string;
  params: LimitOrderParams;
  status: OrderStatus;
  createdAt: number;
  filledAt?: number;
  filledAmount?: number;
  filledPrice?: number;
  txSignature?: string;
  relatedOrderId?: string; // для take profit / stop loss
}

export interface ILimitOrderManager {
  name: string;
  dex: string;

  createOrder(params: LimitOrderParams): Promise<string>;
  cancelOrder(orderId: string): Promise<void>;
  getOrder(orderId: string): Promise<LimitOrder | null>;
  getAllOrders(): Promise<LimitOrder[]>;
  getActiveOrders(): Promise<LimitOrder[]>;
  monitorOrders(): Promise<void>; // для виртуальных ордеров
}
```

---

### ЧАСТЬ 4: PumpFunLimitOrderManager

**Файл:** `src/trading/managers/PumpFunLimitOrderManager.ts`

**Требования:**
- Виртуальные лимитные ордера (мониторинг цены + авто-исполнение)
- Автоматический take profit (создание sell ордера при исполнении buy)
- Автоматический stop loss
- Периодический мониторинг цен (каждые 30 секунд)
- Хранение ордеров в JSON файле
- Связь связанных ордеров (buy → take profit)

**Алгоритм работы:**
```
1. Создать ордер и сохранить в файл
2. Каждые 30 сек проверять текущую цену токена
3. Если условие выполнено → исполнить ордер
4. При исполнении buy с takeProfit → создать sell ордер
5. При отмене buy → отменить связанный sell
6. Обновить статус ордера в файле
```

**Расчет цены:**
```
Buy ордер: исполнить, если currentPrice <= targetPrice
Sell ордер: исполнить, если currentPrice >= targetPrice

Take Profit: sellPrice = buyPrice × (1 + takeProfit% / 100)
Stop Loss: sellPrice = buyPrice × (1 - stopLoss% / 100)
```

---

### ЧАСТЬ 5: PriceMonitor

**Файл:** `src/trading/managers/PriceMonitor.ts`

**Требования:**
- Мониторинг цен для нескольких токенов
- Периодическое обновление цен (каждые 30 секунд)
- Уведомление при достижении целевой цены
- Кэширование цен для оптимизации

**API:**
```typescript
export class PriceMonitor {
  getCurrentPrice(tokenMint: string): Promise<number>; // цена в SOL за 1 токен
  startMonitoring(tokenMints: string[], callback: (mint: string, price: number) => void): void;
  stopMonitoring(): void;
}
```

---

### ЧАСТЬ 6: Интеграция в bot.ts с кнопками

**Файл:** `src/bot.ts`

**Управление через Inline Keyboards:**

#### Главное меню торговли:
```
📊 Торговля
├─ 🛒 Купить токен
├─ 📈 Продать токен
├─ 💹 Котировка
├─ 📋 Лимитные ордера
└─ ⚙️ Настройки
```

#### Меню лимитных ордеров:
```
📋 Лимитные ордера
├─ ➕ Создать ордер
├─ 📜 Мои ордера
├─ ❌ Отменить все
└─ 🔙 Назад
```

#### Настройки:
```
⚙️ Настройки
├─ 📊 Slippage: [1.0%] ▼
├─ 🛡️ MEV защита: [✅] 🔄
├─ ⚡ Скорость: [Normal] ▼
└─ 🔙 Назад
```

**Сценарии использования:**

1. **Создание лимитного ордера:**
   - Нажать "➕ Создать ордер"
   - Ввести mint адрес токена
   - Выбрать тип (Купить/Продать) через кнопки
   - Ввести количество и цену
   - Опционально: указать take profit %
   - Подтвердить создание

2. **Просмотр ордеров:**
   - Нажать "📜 Мои ордера"
   - Показать список с кнопками для каждого ордера:
     - ❌ Отменить
     - ℹ️ Детали

3. **Изменение настроек:**
   - Нажать на параметр для изменения
   - Выбрать значение из списка кнопок

---

### ЧАСТЬ 7: Обновление UserSettings

**Файл:** `src/trading/router/ITradingStrategy.ts`

**Добавить:**
```typescript
export interface UserSettings {
  slippage: number;
  mevProtection: boolean;
  speedStrategy: 'low' | 'normal' | 'aggressive';
  useJito: boolean; // ⭐ НОВОЕ
  jitoTipMultiplier: number; // ⭐ НОВОЕ (например, 1.0 = стандартный tip)
}
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
src/
├── trading/
│   ├── managers/
│   │   ├── PriorityFeeManager.ts (обновить)
│   │   ├── ILimitOrderManager.ts (новый)
│   │   ├── PumpFunLimitOrderManager.ts (новый)
│   │   └── PriceMonitor.ts (новый)
│   ├── strategies/
│   │   └── solana/
│   │       └── PumpFunStrategy.ts (обновить)
│   └── router/
│       └── ITradingStrategy.ts (обновить UserSettings)
├── panels/
│   └── TradingPanel.ts (новый - управление торговлей через кнопки)
├── data/
│   └── limit_orders.json (новый - хранение ордеров)
└── bot.ts (обновить)
```

---

## ✅ ЧЕКЛИСТ

- [ ] Реализовать PriorityFeeManager с getRecentPrioritizationFees
- [ ] Обновить PumpFunStrategy для использования встроенной Jito поддержки
- [ ] Создать интерфейс ILimitOrderManager
- [ ] Реализовать PumpFunLimitOrderManager с виртуальными ордерами
- [ ] Реализовать PriceMonitor для мониторинга цен
- [ ] Создать TradingPanel с inline keyboards для управления торговлей
- [ ] Интегрировать лимитные ордера в TradingPanel
- [ ] Добавить настройки (slippage, MEV, скорость) в TradingPanel
- [ ] Интегрировать все компоненты в bot.ts
- [ ] Протестировать всю систему
- [ ] Обновить отчет

---

## 🎯 ПРИОРИТЕТЫ

1. **Высокий приоритет:**
   - PriorityFeeManager (критично для скорости)
   - PumpFunStrategy с MEV защитой (критично для безопасности)

2. **Средний приоритет:**
   - ILimitOrderManager интерфейс
   - PumpFunLimitOrderManager
   - PriceMonitor

3. **Низкий приоритет:**
   - Дополнительные команды бота
   - Stop Loss (опционально)

---

**Примечание:** Jupiter лимитные ордера будут реализованы позже через Jupiter Limit Order Program.
