# Этап 1: Настройка проекта и базовый бот

## 🎯 Цели

1.  Создать базовую структуру проекта.
2.  Инициализировать `package.json` и установить все необходимые зависимости.
3.  Настроить переменные окружения (`.env`).
4.  Создать основной файл бота (`src/bot.ts`) с базовой командой `/start`.
5.  Реализовать базовую систему безопасности: whitelist для пользователей Telegram и rate limiting.

## 📁 Структура файлов

```
DEX_BOT/
├── plans/
│   └── 01_project_setup.md
├── src/
│   ├── config/
│   ├── types/
│   ├── utils/
│   ├── trading/
│   ├── wallet/
│   └── bot.ts
├── .env
├── .gitignore
├── package.json
└── tsconfig.json
```

## 📝 Ключевые шаги реализации

1.  **`package.json`**:
    -   Создать файл `package.json` с помощью `npm init -y`.
    -   Добавить все зависимости из `solana_dex_guide_v3.md` (раздел "Технический стек").
    -   Добавить скрипты для запуска: `start` и `dev`.

2.  **`tsconfig.json`**:
    -   Настроить компилятор TypeScript с `target: "es2020"`, `module: "commonjs"`, `outDir: "./dist"`.

3.  **`.gitignore`**:
    -   Добавить `node_modules`, `dist`, `.env` и `*.log` в `.gitignore`.

4.  **`.env`**:
    -   Создать файл `.env` на основе шаблона из гайда.
    -   Заполнить `TELEGRAM_BOT_TOKEN` и `ALLOWED_TELEGRAM_USERS`.

5.  **`src/bot.ts`**:
    -   Импортировать `Telegraf` и `dotenv`.
    -   Инициализировать бота с токеном из `.env`.
    -   Реализовать middleware для проверки `ALLOWED_TELEGRAM_USERS`.
    -   Добавить middleware `telegraf-ratelimit` для ограничения запросов (10 команд в минуту).
    -   Создать обработчик для команды `/start`, который будет приветствовать пользователя.
    -   Запустить бота с помощью `bot.launch()`.