# Этап 2: Управление кошельком

## 🎯 Цели

1.  Реализовать безопасное хранение приватных ключей с использованием шифрования (`aes-256-cbc`).
2.  Создать класс `WalletManager` для управления кошельками.
3.  Реализовать команды Telegram для управления кошельком:
    -   `/create_wallet`
    -   `/import_wallet [privateKey]`
    -   `/address`
    -   `/balance`

## 📁 Структура файлов

```
DEX_BOT/
└── src/
    ├── wallet/
    │   └── WalletManager.ts
    ├── types/
    │   └── index.ts
    └── bot.ts
```

## 📝 Ключевые шаги реализации

1.  **`src/wallet/WalletManager.ts`**:
    -   Создать класс `SecureWalletStorage` внутри `WalletManager.ts` (или в отдельном файле `utils/SecureStorage.ts`), как описано в `solana_dex_guide_v3.md` (раздел 2). Этот класс будет отвечать за шифрование и дешифрование приватных ключей с помощью `MASTER_PASSWORD` из `.env`.
    -   Создать класс `WalletManager`.
    -   **`createWallet()`**:
        -   Генерирует новый `Keypair` с помощью `@solana/web3.js`.
        -   Шифрует приватный ключ с помощью `SecureWalletStorage`.
        -   Сохраняет зашифрованный ключ (например, в JSON файл или в простую базу данных in-memory для начала).
        -   Возвращает публичный ключ.
    -   **`importWallet(privateKey: string)`**:
        -   Принимает приватный ключ в формате `bs58`.
        -   Шифрует и сохраняет его.
        -   Возвращает публичный ключ.
    -   **`getWallet()`**:
        -   Загружает зашифрованный ключ.
        -   Дешифрует его.
        -   Возвращает `Keypair`.
    -   **`getBalance()`**:
        -   Получает `Keypair` с помощью `getWallet()`.
        -   Использует `connection.getBalance()` для получения баланса SOL.
        -   (На этом этапе только SOL, баланс токенов будет добавлен позже).

2.  **`src/bot.ts`**:
    -   Импортировать `WalletManager`.
    -   Инициализировать `walletManager`.
    -   **`/create_wallet`**:
        -   Вызвать `walletManager.createWallet()`.
        -   Ответить пользователю сообщением об успешном создании и показать публичный адрес.
    -   **`/import_wallet`**:
        -   Извлечь приватный ключ из сообщения.
        -   Вызвать `walletManager.importWallet(privateKey)`.
        -   Ответить пользователю сообщением об успехе. **Удалить исходное сообщение пользователя с ключом для безопасности.**
    -   **`/address`**:
        -   Получить адрес через `walletManager.getWallet().publicKey`.
        -   Отправить адрес пользователю.
    -   **`/balance`**:
        -   Вызвать `walletManager.getBalance()`.
        -   Ответить пользователю его балансом SOL.