# Пошаговый план тестирования торговой панели

**Время на тестирование:** 2-4 часа  
**Рекомендация:** Тестировать на devnet/testnet Solana

---

## 🎯 ЭТАП 0: Подготовка к тестированию

### 1. Создать тестовую БД
```bash
# Удалить старую БД если есть
rm dev.db

# Создать новую схему
npx prisma migrate dev --name init

# Проверить что БД пустая
npx prisma studio
```

### 2. Настроить переменные окружения
```bash
# .env
NODE_ENV=development
TELEGRAM_BOT_TOKEN=your_test_bot_token
ALCHEMY_SOLANA_RPC=your_rpc_url

# Важно: используйте тестовый бот и кошелек с минимальным балансом!
ALLOWED_TELEGRAM_USERS=your_user_id
```

### 3. Добавить тестовое логирование
```typescript
// В начало bot.ts добавить
console.log('🧪 TESTING MODE ENABLED');
console.log('📊 Database:', process.env.DATABASE_URL || 'file:./dev.db');
console.log('👥 Allowed users:', ALLOWED_USERS);
```

---

## 🧪 ЭТАП 1: Базовый функционал (15 мин)

### Тест 1.1: Запуск бота
```bash
npm run dev
```

**✅ Ожидаем:**
```
🗄️ Connecting to database...
✅ Database connected
🚀 Starting bot initialization...
✅ Wallet loaded: [address]
💰 Balance: X.XXXX SOL
✅ StateManager initialized.
✅ TokenDataFetcher initialized.
✅ PositionTracker initialized.
✅ TPSLManager initialized.
✅ Trading panel initialized.
✅ AutoRefreshService initialized.
✅ Bot running!
```

**❌ Если ошибка:**
- Проверить что Prisma схема применена
- Проверить что кошелек создан/импортирован
- Проверить RPC URL

---

### Тест 1.2: Открытие панели
**Действия:**
1. Отправить боту адрес любого токена из Solana
2. Например: `EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v` (USDC)

**✅ Ожидаем:**
- Сообщение "⏳ Загрузка данных токена..."
- Панель открывается с данными токена
- Кнопки режимов: Buy, Sell, Limit, Track
- Баланс отображается корректно

**📝 Логи для проверки:**
```
💬 Received text message: EPjF...
[TokenDataFetcher] Loaded X tokens from Jupiter token list
[StateManager] Set state for user 123456
✅ Trading panel opened for token EPjF...
[AutoRefreshService] Started auto-refresh for user 123456
```

**❌ Если ошибка:**
- Проверить что `extractSolanaAddress()` работает
- Проверить что TokenDataFetcher загрузил token list

---

### Тест 1.3: Авто-обновление
**Действия:**
1. Открыть панель токена
2. Подождать 10 секунд
3. Наблюдать за обновлением цены

**✅ Ожидаем:**
- Панель обновляется каждые 5 секунд
- Цена токена меняется (если рынок активен)
- Баланс обновляется

**📝 Логи для проверки:**
```
[AutoRefreshService] Refreshing panel for user 123456
[TokenDataFetcher] Fetching token data for EPjF...
[AutoRefreshService] Error updating balance: [не должно быть!]
```

**❌ Если ошибка "Error updating balance":**
- Проверить что walletManager и solanaProvider переданы в AutoRefreshService
- Проверить что кошелек доступен

---

### Тест 1.4: Переключение режимов
**Действия:**
1. Нажать "Sell"
2. Нажать "Limit"
3. Нажать "Track"
4. Вернуться на "Buy"

**✅ Ожидаем:**
- Режимы переключаются
- Кнопки действий меняются для каждого режима
- Текст панели обновляется

**❌ Если не работает:**
- Проверить `handleCallback` → `handleModeChange`

---

## 💰 ЭТАП 2: Покупка (30 мин)

### Тест 2.1: Выбор суммы
**Действия:**
1. Открыть панель токена
2. Нажать "$10"
3. Нажать "$50"
4. Нажать "$100"

**✅ Ожидаем:**
- Текст обновляется: "Selected: $10" → "$50" → "$100"
- Панель не ломается

---

### Тест 2.2: Покупка с недостаточным балансом
**Действия:**
1. Выбрать сумму больше баланса (например $1000)
2. Нажать "🟢 Execute Trade"

**✅ Ожидаем:**
- Ошибка: "Insufficient balance. Required: $1000, Available: $X"
- Панель не ломается

**📝 Логи:**
```
[TradingPanel] Error handling callback: Insufficient balance...
```

---

### Тест 2.3: Покупка на $10 (КРИТИЧНЫЙ ТЕСТ!)
**Действия:**
1. Выбрать сумму $10
2. Нажать "🟢 Execute Trade"

**✅ Ожидаем:**
```
[TradingPanel] Buying for $10 (0.0556 SOL) at rate 180
[TradingPanel] Buy executed: 123.4567 tokens at 0.00001234 SOL
[PositionTracker] Recorded BUY trade for user 123456
```

**🔍 ПРОВЕРИТЬ В ЛОГАХ:**
- Конвертация USD → SOL правильная
- Баланс уменьшился на ~0.0556 SOL
- Позиция создана в БД

**❌ Если сумма неправильная:**
- Проверить что `solPriceUSD` не null
- Проверить формулу: `amountSOL = amountUSD / solPriceUSD`

---

### Тест 2.4: Проверка баланса после покупки
**Действия:**
1. Запомнить баланс до покупки
2. Купить на $10
3. Проверить баланс в панели

**✅ Ожидаем:**
- Баланс уменьшился на сумму покупки + комиссия
- `user_data.sol_balance` обновился
- `user_data.usd_balance` обновился

**🔍 Проверить в БД:**
```bash
npx prisma studio
# Открыть UserPanelState → userData → sol_balance
```

---

### Тест 2.5: Проверка позиции после покупки
**Действия:**
1. Купить токен на $10
2. Переключиться в режим "Track"

**✅ Ожидаем:**
- Отображается позиция:
  ```
  📈 Position Tracking
  Entry: 0.00001234 SOL
  Current: 0.00001240 SOL
  Size: 123.45 tokens
  PNL: 🟢 $0.05 (+0.5%)
  ```

**🔍 Проверить в БД:**
```bash
npx prisma studio
# Position → userId, tokenAddress, entryPrice, size
# Trade → type=BUY, price, size
```

---

## 📉 ЭТАП 3: Продажа (30 мин)

### Тест 3.1: Продажа без позиции
**Действия:**
1. Открыть токен, который не покупали
2. Переключиться в "Sell"
3. Нажать "🔴 Execute Trade"

**✅ Ожидаем:**
- Ошибка: "No active position found to sell."

---

### Тест 3.2: Продажа 50%
**Действия:**
1. Купить токен на $20
2. Переключиться в "Sell"
3. Нажать "50%"
4. Нажать "🔴 Execute Trade"

**✅ Ожидаем:**
```
[TradingPanel] Sell executed for 50% of position (61.7283 tokens)
[PositionTracker] Recorded SELL trade for user 123456
New position size: 61.7283
```

**🔍 ПРОВЕРИТЬ:**
- Размер позиции уменьшился в 2 раза
- Баланс SOL увеличился
- В БД создана запись Trade с type=SELL

---

### Тест 3.3: Продажа 100%
**Действия:**
1. Купить токен
2. Продать 100%

**✅ Ожидаем:**
```
[TradingPanel] Sell executed for 100% of position
[TradingPanel] Position for [token] closed.
New position size: 0
```

**🔍 ПРОВЕРИТЬ:**
- Позиция закрыта (size = 0)
- В режиме "Track" показывается "No open position"

---

### Тест 3.4: Валидация процента
**Действия:**
1. Открыть "Sell"
2. Ввести в коде `selected_amount = 150` (через handleAmountChange)
3. Попробовать продать

**✅ Ожидаем:**
- Ошибка: "Invalid sell percentage. Must be between 0 and 100."

---

## 🎯 ЭТАП 4: Лимитные ордера (45 мин)

### Тест 4.1: Создание лимитного ордера
**Действия:**
1. Открыть панель токена
2. Переключиться в "Limit"
3. Нажать "Set Price"
4. Ввести цену (например 0.00005)
5. Нажать "Set Amount"
6. Ввести сумму (например 20)
7. Нажать "📍 Place Order"

**✅ Ожидаем:**
```
[TradingPanel] Limit order placed: [orderId] for user 123456
[TradingPanel] Buying for $20 (0.1111 SOL) at rate 180
```

**🔍 КРИТИЧНО ПРОВЕРИТЬ:**
- `amount` в ордере = lamports (не USD!)
- `userId` сохранен в ордере
- `activeLimitOrderId` сохранен в state

**📝 Проверить в логах LimitOrderManager:**
```
[PumpFunLimitOrderManager] Creating order with params:
  userId: 123456
  amount: 111100000 (lamports)
  price: 0.00005
```

---

### Тест 4.2: Отмена лимитного ордера
**Действия:**
1. Создать лимитный ордер
2. Нажать "❌ Cancel Order"

**✅ Ожидаем:**
```
[TradingPanel] Limit order cancelled: [orderId] for user 123456
[TPSLManager] Cancelled all related orders for position [orderId]
```

**🔍 ПРОВЕРИТЬ:**
- Ордер отменен в блокчейне
- `has_active_order = false`
- `activeLimitOrderId = undefined`

---

### Тест 4.3: Исполнение лимитного ордера (КРИТИЧНЫЙ!)
**Действия:**
1. Создать лимитный ордер
2. Дождаться исполнения (или симулировать)

**✅ Ожидаем:**
```
[Bot] Handling filled limit order [orderId]...
[Bot] Order filled for user 123456: [orderId]
[PositionTracker] Recorded BUY trade for user 123456
```

**🔍 КРИТИЧНО ПРОВЕРИТЬ:**
- Позиция записана на ПРАВИЛЬНОГО пользователя
- Не используется `ALLOWED_USERS[0]`
- В логах есть `userId` из ордера

**❌ Если позиция на другом пользователе:**
- Проверить что `order.params.userId` существует
- Проверить что LimitOrderManager сохраняет userId

---

## 🎯 ЭТАП 5: TP/SL (45 мин)

### Тест 5.1: Создание позиции с TP/SL
**Действия:**
1. Открыть панель
2. Нажать "🎯 TP/SL"
3. Должны установиться TP +50%, SL -10%
4. Купить токен на $20

**✅ Ожидаем:**
```
[TradingPanel] Buy executed: 123.45 tokens at 0.00001234 SOL
[TPSLManager] Creating TP/SL orders for position [positionId]
[TPSLManager] Created and linked TP/SL orders for position [positionId]
```

**🔍 ПРОВЕРИТЬ В БД:**
```bash
npx prisma studio
# LinkedOrder → positionId, tpOrderId, slOrderId
```

---

### Тест 5.2: Проверка размера TP/SL ордеров
**Действия:**
1. Создать позицию 100 токенов с TP/SL
2. Проверить логи

**✅ Ожидаем:**
```
[TPSLManager] Creating TP order: 
  amount: 100000000000 (базовые единицы для 100 токенов)
  price: 0.00001851 (entryPrice * 1.5)
[TPSLManager] Creating SL order:
  amount: 100000000000
  price: 0.00001111 (entryPrice * 0.9)
```

**🔍 КРИТИЧНО:**
- `amount` должен быть в базовых единицах (учитывая decimals)
- НЕ должно быть просто `size` из позиции

---

### Тест 5.3: Исполнение TP ордера
**Действия:**
1. Создать позицию с TP/SL
2. Дождаться исполнения TP (или симулировать)

**✅ Ожидаем:**
```
[TPSLManager] TP filled. Canceled opposite order [slOrderId]
[PositionTracker] Recorded SELL trade
```

**🔍 ПРОВЕРИТЬ:**
- SL ордер отменен
- Позиция закрыта
- В БД LinkedOrder удален

---

### Тест 5.4: Race condition при одновременном TP и SL
**Действия:**
1. Создать позицию с TP/SL
2. В коде симулировать одновременное срабатывание:
```typescript
// В тестовом файле
await Promise.all([
  tpslManager.onOrderFilled(tpOrderId),
  tpslManager.onOrderFilled(slOrderId)
]);
```

**✅ Ожидаем:**
- Только один ордер обработан
- Нет двойной отмены
- Нет ошибок в логах

---

## 👥 ЭТАП 6: Мультиюзер (30 мин)

### Тест 6.1: Два пользователя, разные токены
**Действия:**
1. Пользователь A открывает токен X
2. Пользователь B открывает токен Y
3. Оба покупают

**✅ Ожидаем:**
- Панели независимые
- Позиции не смешиваются
- Балансы отдельные

**🔍 ПРОВЕРИТЬ В БД:**
```sql
SELECT * FROM Position WHERE userId = [userA_id];
SELECT * FROM Position WHERE userId = [userB_id];
```

---

### Тест 6.2: Два пользователя, один токен
**Действия:**
1. Оба открывают USDC
2. Оба покупают на $10
3. Оба создают лимитные ордера

**✅ Ожидаем:**
- Каждый видит свою позицию
- Лимитные ордера с правильными userId

---

### Тест 6.3: Исполнение лимитных ордеров разных пользователей
**Действия:**
1. Пользователь A создает лимитный ордер на токен X
2. Пользователь B создает лимитный ордер на токен X
3. Оба ордера исполняются

**✅ Ожидаем:**
```
[Bot] Order filled for user [A_id]: [orderA_id]
[Bot] Order filled for user [B_id]: [orderB_id]
```

**🔍 КРИТИЧНО:**
- Позиция A записана на пользователя A
- Позиция B записана на пользователя B
- НЕТ записей на `ALLOWED_USERS[0]`

---

## 🔄 ЭТАП 7: Восстановление (15 мин)

### Тест 7.1: Перезапуск бота
**Действия:**
1. Открыть панель токена
2. Остановить бота (Ctrl+C)
3. Запустить снова

**✅ Ожидаем:**
```
[AutoRefreshService] Restoring all active panels...
[AutoRefreshService] Restored panel for user 123456, token [address]
[AutoRefreshService] Restore complete: 1 panels restored, 0 panels cleaned up
```

**🔍 ПРОВЕРИТЬ:**
- Панель все еще работает
- Авто-обновление возобновилось
- Кнопки работают

---

### Тест 7.2: Очистка старых панелей
**Действия:**
1. В БД вручную изменить `createdAt` панели на 2 часа назад
2. Подождать 1 минуту (или вызвать `cleanupInactiveStates()`)

**✅ Ожидаем:**
```
[StateManager] Cleaned up 1 inactive states from DB.
```

**🔍 ПРОВЕРИТЬ В БД:**
- Старая панель удалена
- Новые панели остались

---

## 📊 ЭТАП 8: Нагрузочное тестирование (опционально, 30 мин)

### Тест 8.1: Быстрые действия
**Действия:**
1. Быстро нажимать кнопки в панели (10 раз за 5 секунд)

**✅ Ожидаем:**
- Rate limiting срабатывает: "Please wait X seconds between trades"
- Бот не падает
- Панель не ломается

---

### Тест 8.2: Много панелей одновременно
**Действия:**
1. Открыть 5 разных токенов
2. Оставить на 1 минуту

**✅ Ожидаем:**
- Все панели обновляются
- Нет ошибок в логах
- CPU/память в норме

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

### Критический функционал
- [ ] Бот запускается без ошибок
- [ ] Панель открывается по адресу токена
- [ ] Авто-обновление работает каждые 5 секунд
- [ ] Покупка на $10 конвертируется правильно
- [ ] Баланс обновляется после покупки
- [ ] Позиция создается и отображается
- [ ] Продажа 50% работает корректно
- [ ] Продажа 100% закрывает позицию
- [ ] Баланс обновляется после продажи
- [ ] Лимитный ордер создается с правильной суммой (lamports)
- [ ] Лимитный ордер отменяется в блокчейне
- [ ] При исполнении ордера userId правильный (НЕ ALLOWED_USERS[0])
- [ ] TP/SL создаются с правильными размерами (базовые единицы)
- [ ] При исполнении TP отменяется SL
- [ ] Race condition не вызывает ошибок

### Мультиюзер
- [ ] Два пользователя работают независимо
- [ ] Позиции не смешиваются
- [ ] Лимитные ордера с правильными userId

### Восстановление
- [ ] Панели восстанавливаются после перезапуска
- [ ] Старые панели удаляются

---

## 🐛 ЧТО ДЕЛАТЬ ПРИ ОШИБКАХ

### Ошибка: "Could not fetch SOL price"
**Решение:**
1. Проверить интернет
2. Проверить Jupiter API доступен
3. Fallback 180 должен сработать

### Ошибка: "Insufficient balance" при правильном балансе
**Проверить:**
1. `solPriceUSD` не null
2. Формула конвертации правильная
3. Баланс в `user_data` актуальный

### Ошибка: "Order missing userId"
**Проверить:**
1. `ILimitOrderManager` сохраняет userId в params
2. `LimitOrderParams` имеет поле userId
3. При создании ордера userId передается

### Ошибка: Позиция записана не тому пользователю
**КРИТИЧНО! Проверить:**
1. `handleLimitOrderFill` использует `order.params.userId`
2. НЕ используется `ALLOWED_USERS[0]`
3. В логах правильный userId

---

## 📝 ШАБЛОН ОТЧЕТА О ТЕСТИРОВАНИИ

```markdown
# Отчет о тестировании

**Дата:** [дата]
**Версия:** 1.0
**Тестировщик:** [имя]

## Пройденные тесты

### ✅ Базовый функционал
- [x] Запуск бота
- [x] Открытие панели
- [x] Авто-обновление
- [x] Переключение режимов

### ✅ Покупка
- [x] Выбор суммы
- [x] Валидация баланса
- [x] Покупка на $10
- [x] Обновление баланса
- [x] Создание позиции

### ✅ Продажа
- [x] Продажа без позиции (ошибка)
- [x] Продажа 50%
- [x] Продажа 100%
- [x] Валидация процента

### ✅ Лимитные ордера
- [x] Создание ордера
- [x] Отмена ордера
- [x] Исполнение ордера
- [x] Правильный userId

### ✅ TP/SL
- [x] Создание с позицией
- [x] Правильный размер ордеров
- [x] Исполнение TP
- [x] Race condition

### ✅ Мультиюзер
- [x] Независимые панели
- [x] Независимые позиции
- [x] Лимитные ордера

### ✅ Восстановление
- [x] После перезапуска
- [x] Очистка старых

## Найденные проблемы
[список проблем или "Нет"]

## Рекомендации
[рекомендации или "Готово к продакшену"]
```

---

**Удачи в тестировании!** 🚀